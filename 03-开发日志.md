# 03-开发日志

- 初始化：创建项目目录与 Git 仓库，建立任务文档。
- 2026-02-16 M1：完成 App Router 页面、文案生成 API、schema 校验、全局样式与项目配置文件。
- 2026-02-16 M1：补充 README、.env.example、smoke 脚本与 npm scripts（dev/build/start/lint/smoke）。
- 2026-02-16 M1：执行 `npm run build` 与 `bash scripts/smoke.sh` 验证。
- 2026-02-16 修复：将 chat/completions 默认模型从 `gpt-4o-mini` 调整为 `supermind-agent-v1`，并更新 `scripts/smoke.sh` 默认模型与 `.env.local/.env` 自动加载逻辑，解决 Unsupported model 导致的 smoke 失败。
- 2026-02-16 M2：新增 `POST /api/couplet/poster`，接入 `https://space.ai-builders.com/backend/v1/images/generations`，默认图片模型 `gpt-image-1.5`。
- 2026-02-16 M2：前端新增“生成海报图/下载海报图”能力，支持图片预览；图片生成失败时给出明确错误提示且不影响春联文本结果使用。
- 2026-02-16 M3：完成移动端优化（390px 可用、按钮高度 >=44px、输入/结果/海报区防溢出）、加载态与禁用态优化，避免重复提交。
- 2026-02-16 M3：后端 `generate/poster` 接口统一返回 `requestId`（成功/失败），新增最小化日志（`requestId/durationMs/statusCode/errorType`）与超时控制（默认 30s，限制在 25~40s），并返回明确超时错误。
- 2026-02-16 M3：补充交付文档：README（M1~M3说明、移动端建议、错误排查、poster curl 示例）、`docs/mobile-checklist.md`、测试验收记录追加。
